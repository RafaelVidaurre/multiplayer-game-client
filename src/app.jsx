import React from 'react';

import Stage from './components/stage/stage.jsx';

export default React.createClass({
  render () {
    return (
      <div id="app">
        <Stage/>
      </div>
    );
  }
});
